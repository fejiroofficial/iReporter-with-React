language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm test
after_script:
  - npm run coverage
  - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT